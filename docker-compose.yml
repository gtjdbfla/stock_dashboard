services:
  hynix-dashboard:
    build: .
    container_name: hynix-dashboard
    ports:
      - "8501:8501"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
