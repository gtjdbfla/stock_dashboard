# 작업 전 필독

1. `CONTEXT.md` — 목적·배포·확정된 결정·반복해서 걸린 함정. **먼저 읽는다.**
2. `MAP.md` — app.py(380KB·함수 154개) 줄 번호 지도.

**app.py를 통째로 읽지 말 것.** MAP.md에서 줄 번호를 찾아 그 범위만 연다.
새로 알게 된 사실은 CONTEXT.md에 쓴다(§4 결정 / §5 함정). 변경 이력은 쓰지 않는다 — git log 담당.
